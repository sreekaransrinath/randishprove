name: Create Bot Issues

on:
  workflow_dispatch:
    inputs:
      count:
        description: 'Number of issues to create'
        required: true
        default: '1'
        type: string

permissions:
  contents: read
  issues: write

jobs:
  run-task:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Run Script
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PERSONAL_ACCESS_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        run: |
          python .github/scripts/daily_ops.py --action create_bot_issues --count ${{ inputs.count }}


